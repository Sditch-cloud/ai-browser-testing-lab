{
  "name": "parse-testcase",
  "version": "1.0.0",
  "description": "Parse a YAML test case file and return a structured test definition.",
  "parameters": {
    "type": "object",
    "required": ["testName"],
    "properties": {
      "testName": {
        "type": "string",
        "description": "Name of the test case (without extension) located in tests/cases/"
      },
      "casesDir": {
        "type": "string",
        "description": "Optional override for the test cases directory path",
        "default": "tests/cases"
      }
    }
  },
  "returns": {
    "type": "object",
    "description": "Parsed test case with metadata, steps, and assertions",
    "properties": {
      "metadata": { "type": "object" },
      "steps": { "type": "array" },
      "assertions": { "type": "array" }
    }
  }
}

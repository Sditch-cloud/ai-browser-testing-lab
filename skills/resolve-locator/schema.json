{
  "name": "resolve-locator",
  "version": "1.0.0",
  "description": "Resolve selector-free test steps or assertions into a Playwright locator descriptor.",
  "parameters": {
    "type": "object",
    "required": ["descriptor"],
    "properties": {
      "descriptor": {
        "type": "object",
        "description": "A test step or assertion that may contain selector, testId, label, role/name, text, placeholder, target, or description"
      },
      "context": {
        "type": "object",
        "description": "Optional browser context. Provide { page } to enable DOM-based inference."
      }
    }
  },
  "returns": {
    "type": "object",
    "properties": {
      "locator": { "type": "object", "description": "Resolved locator descriptor" },
      "strategy": { "type": "string", "description": "How the locator was resolved" },
      "detail": { "type": "string", "description": "Human-readable resolution summary" }
    }
  }
}
